'use strict';

describe('Directive: editorDirectives', function () {

  // load the directive's module
  beforeEach(module('showcaseEditor.directives'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
